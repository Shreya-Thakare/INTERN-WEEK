const express=require('express'),cors=require('cors');const routes=require('./routes/employeeRoutes');const errorHandler=require('./middleware/errorHandler');const app=express();app.use(cors());app.use(express.json());app.get('/',(req,res)=>res.json({message:'Employee API running'}));app.use('/api/employees',routes);app.use(errorHandler);app.listen(process.env.PORT||4000,()=>console.log('API at http://localhost:4000'));
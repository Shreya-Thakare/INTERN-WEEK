import Link from 'next/link';const api=process.env.NEXT_PUBLIC_API_URL||'http://localhost:4000/api';export default async function Employees(){let employees=[];try{employees=await fetch(`${api}/employees`,{cache:'no-store'}).then(r=>r.json())}catch{}return <main><h1>Employees</h1><Link href="/employees/create">Create employee</Link><ul>{employees.map(e=><li key={e.id}><Link href={`/employees/${e.id}`}>{e.name}</Link> — {e.department} — ₹{e.salary}</li>)}</ul>{!employees.length&&<p>Unable to load employees. Start the Node API.</p>}</main>}
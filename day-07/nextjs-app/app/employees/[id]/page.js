import Link from 'next/link';const api=process.env.NEXT_PUBLIC_API_URL||'http://localhost:4000/api';export default async function Details({params}){const {id}=await params;const res=await fetch(`${api}/employees/${id}`,{cache:'no-store'});if(!res.ok)return <p>Employee not found. <Link href="/employees">Back</Link></p>;const e=await res.json();return <main><Link href="/employees">← Employees</Link><h1>{e.name}</h1><p>Email: {e.email}</p><p>Department: {e.department}</p><p>Position: {e.position}</p><p>Salary: ₹{e.salary}</p></main>}
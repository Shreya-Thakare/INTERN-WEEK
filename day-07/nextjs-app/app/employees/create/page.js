'use client';import {useRouter} from 'next/navigation';import {useState} from 'react';const api=process.env.NEXT_PUBLIC_API_URL||'http://localhost:4000/api';export default function Create(){const r=useRouter();const [message,setMessage]=useState('');async function submit(e){e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));d.salary=Number(d.salary);const res=await fetch(`${api}/employees`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(d)});if(res.ok)r.push('/employees');else setMessage('Please enter all valid fields.')}return <main><h1>Create employee</h1><form onSubmit={submit}>{['name','email','department','position','salary'].map(x=><p key={x}><input name={x} type={x==='salary'?'number':x==='email'?'email':'text'} placeholder={x} required min={x==='salary'?1:undefined}/></p>)}<button>Save</button></form><p>{message}</p></main>}